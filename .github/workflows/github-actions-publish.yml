name: Publish Image To DockerHub
on:
  push:
    branches:
      - 'action-start'
    tags:
      - '*'

jobs:
  docker:
    environment: default
    env:
      IMAGE_TAG: ${{ secrets.DOCKERHUB_USERNAME }}/${GITHUB_REPOSITORY##*/}:${GITHUB_REF##*/}
    runs-on: ubuntu-latest
    steps:
      - name: Show GITHUB_REF value
        run: echo "($IMAGE_TAG) (${GITHUB_REF}) (${GITHUB_REF#refs/*/}) (${GITHUB_REF##*/}) (${GITHUB_REPOSITORY}) (${GITHUB_REPOSITORY##*/})"
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push
        uses: docker/build-push-action@v3
        with:
          push: true
          tags: ${{ IMAGE_TAG }}
